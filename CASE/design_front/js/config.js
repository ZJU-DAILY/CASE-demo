/**
 * js/config.js
 * 
 * 应用程序的静态配置文件。
 * 这里存放不经常改变的常量，如 API 地址、颜色主题等。
 * 其他模块可以从这里导入配置，而不是在代码中硬编码。
 */

// 后端 API 的基础 URL
export const API_BASE_URL = "http://10.214.131.13:5019";
// export const API_BASE_URL = "http://localhost:5019";

// 定义亮色和暗色主题的颜色面板
// 将所有颜色集中管理，便于统一调整视觉风格
// 将所有颜色集中管理，便于统一调整视觉风格
export const THEME_PALETTES = {
    light: {
        // --- 节点颜色 (Node Colors) ---
        seedNode: '#818CF8',                // 种子节点 (保留了原有的亮紫色，非常醒目)
        blockingNode: '#006400',            // 阻塞节点 (改为明亮的青绿色，清晰地区分)

        /**
         * 影响程度渐变色 (暖色调) - 区分度增强
         * 调整了色相和饱和度，让每个层级的颜色特征更鲜明。
         */
        hot: '#FF6B6B',                 // 高影响力 (更鲜明的西瓜红，警示度更高)
        mid: '#FFA500',                 // 中影响力 (更饱和的阳光橙，充满活力)
        cold: '#FFFACD',                // 低影响力 (改为清新的青柠色，与中高影响力的颜色拉开差距)

        /**
         * 社群影响程度渐变色 (冷色调) - 区分度增强
         * 增大了色相的跨度，从紫-蓝-青的过渡更清晰。
         */
        community: {
            hot: '#B36BFF',             // 高影响力社群 (更浓郁的紫水晶色，突出重点)
            mid: '#29B6F6',             // 中影响力社群 (更纯粹的天空蓝，作为清晰的中间态)
            cold: '#B0E2FF',            // 低影响力社群 (更明亮的薄荷绿，清新且易于分辨)
        },

        recovered: '#76DA91',           // 恢复的节点 (改为生机勃勃的亮绿色)
        inactive: '#D1D5DB',            // 未激活的普通节点 (更浅的灰色，避免干扰)
        default: '#E5E7EB',             // 默认节点 (保持原样，作为最基础的底色)
        cutOff: 'rgba(200, 40, 40, 1)', // 被切断的节点/路径 (使用更明亮的红色并调整透明度)

        // --- 边与粒子颜色 (Link & Particle Colors) ---
        propagation: '#CDBE70',         // 主要传播路径 (使用中性浅灰色，不抢占视觉焦点)
        criticalPath: '#EC4899',        // 关键传播链 (保留原有的亮粉色，非常适合作为关键路径)
        activatedLink: '#63B2EE',       // 已激活的边 (清爽的天蓝色，视觉引导清晰)
        inactivatedLink: 'rgba(176, 176, 176, 0.4)', // 未激活的边 (非常浅的灰色，弱化显示)
        particle: '#A3E635',            // 传播粒子 (保留原有的亮绿色，动态效果醒目)

        // --- 背景色 ---
        background: '#FFFFFF',          // 背景色 (保持白色)
    }
};